spring:
  datasource:
    url: jdbc:mysql://localhost:3307/enexse-test?createDatabaseIfNotExist=true
    username: root
    password: Bambademe99
    #driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database: MYSQL
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    show-sql: true
server:
  port: 8082
  error:
    include-message: always
